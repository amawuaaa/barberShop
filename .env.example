# SQLite local (ruta relativa a /prisma)
DATABASE_URL="file:./dev.db"

# En Vercel se genera prisma/deploy.db en el build y se copia a /tmp en runtime.
# No hace falta configurar DATABASE_URL manualmente en el dashboard para el demo.

# --- Notificaciones (opcional, stubs listos en src/lib/notifications.ts) ---
# RESEND_API_KEY=
# RESEND_FROM_EMAIL="SIGMABARBER <citas@tudominio.com>"
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_WHATSAPP_FROM="whatsapp:+351932968589"
